- if @current_user.present?
  =breadcrumb_tag do
    %li.active
      = t('headers.users.new')
    %li
      = link_to 'Пользователи', users_path
    %li
      Администрирование

- if check_sudo
  %h2= "Данная роль не имеет привелегий администрирования"
- else    
  .page-header
    %h1= t('headers.users.new')
  = render 'form'

